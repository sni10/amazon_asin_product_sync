services:
  php:
    build:
      args:
        APP_ENV: ${APP_ENV}
    environment:
      APP_ENV: ${APP_ENV}
      XDEBUG_MODE: ${XDEBUG_MODE:-coverage}
    entrypoint: ["/usr/local/bin/entrypoint.sh"]
    command: []
#    volumes:
#      - .:/var/www/amazon_asin_product_sync
